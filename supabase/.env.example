# Edge Function secrets for send-email hook (do not commit real values)
# Set on the hosted project:
#   supabase secrets set --env-file supabase/.env

BREVO_API_KEY=xkeysib-xxxxxxxxxxxxxxxxxxxxxxxx
SEND_EMAIL_HOOK_SECRET="v1,whsec_paste_from_supabase_dashboard"
AUTH_EMAIL_FROM="Power Music Ops <noreply@yourdomain.com>"
AUTH_APP_NAME="Power Music Ops"

# Get BREVO_API_KEY from Brevo → SMTP & API → API keys
# Verify your sender domain in Brevo → Senders, domains & dedicated IPs → Domains
# The from address must match a verified sender/domain in Brevo
